import{NextResponse}from'next/server';import{currentUser}from'@/lib/auth';import{db}from'@/lib/db';import{jsonError}from'@/lib/http';export async function GET(){const a=await currentUser();if(!a||a.role!=='ADMIN')return jsonError('Admin access required',403);const chats=await db.chat.findMany({orderBy:{updatedAt:'desc'},take:100,select:{id:true,title:true,updatedAt:true,user:{select:{name:true,email:true}},_count:{select:{messages:true}}}});return NextResponse.json({chats});}
