import { NextRequest, NextResponse } from 'next/server'; import { currentUser } from '@/lib/auth'; import { db } from '@/lib/db'; import { chatSchema } from '@/lib/schemas'; import { jsonError } from '@/lib/http'; import { activeModel, complete } from '@/lib/ai/provider';
export const maxDuration=120;
export async function POST(req:NextRequest){const user=await currentUser();if(!user||user.banned)return jsonError('Please sign in to chat',401);try{const {chatId,message}=chatSchema.parse(await req.json());let chat=chatId?await db.chat.findFirst({where:{id:chatId,userId:user.id}}):null;if(chatId&&!chat)return jsonError('Chat not found',404);if(!chat)chat=await db.chat.create({data:{userId:user.id,title:message.slice(0,60)}});const [prior,settings]=await Promise.all([db.message.findMany({where:{sessionId:chat.id},orderBy:{createdAt:'asc'},take:40}),db.setting.findUnique({where:{userId:user.id}})]);const userMessage=await db.message.create({data:{sessionId:chat.id,role:'USER',content:message}});const started=Date.now();try{const response=await complete([{role:'system',content:settings?.systemPrompt||'You are a helpful assistant.'},...prior.map(m=>({role:m.role.toLowerCase() as 'user'|'assistant',content:m.content})),{role:'user',content:message}],{temperature:settings?.temperature??0.7,maxTokens:settings?.maxTokens??2048,topP:settings?.topP??1,stream:true});const model=activeModel();const enc=new TextEncoder();let full='';const stream=new ReadableStream({async start(controller){const reader=response.body!.getReader();const decoder=new TextDecoder();try{while(true){const {done,value}=await reader.read();if(done)break;const chunk=decoder.decode(value,{stream:true});for(const line of chunk.split('\n')){if(!line.startsWith('data:'))continue;const data=line.slice(5).trim();if(!data||data==='[DONE]')continue;try{const j=JSON.parse(data);const text=j.choices?.[0]?.delta?.content??j.delta?.text??'';if(text){full+=text;controller.enqueue(enc.encode(`data: ${JSON.stringify({text})}\n\n`));}}catch{}}}const tokens=Math.ceil(full.length/4);await db.$transaction([db.message.create({data:{sessionId:chat!.id,role:'ASSISTANT',content:full,model,tokens}}),db.aiLog.create({data:{userId:user.id,prompt:message,response:full,model,responseTime:Date.now()-started,tokens}}),db.apiUsage.create({data:{userId:user.id,provider:process.env.AI_PROVIDER||'deepseek',tokens}})]);controller.enqueue(enc.encode(`data: ${JSON.stringify({done:true,chatId:chat!.id})}\n\n`));}catch(e){console.error('Chat stream failed',e);controller.enqueue(enc.encode(`data: ${JSON.stringify({error:'Response interrupted. Please retry.'})}\n\n`));}finally{controller.close();}}});return new Response(stream,{headers:{'Content-Type':'text/event-stream','Cache-Control':'no-cache, no-transform','Connection':'keep-alive','X-Accel-Buffering':'no'}});}catch(e){console.error('Chat request failed',e);return jsonError(e instanceof Error?e.message:'Unable to contact AI provider',502);}}catch{return jsonError('Invalid chat request');}}
