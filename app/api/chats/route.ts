import { NextRequest, NextResponse } from 'next/server'; import { currentUser } from '@/lib/auth'; import { db } from '@/lib/db'; import { jsonError } from '@/lib/http';
export async function GET(req:NextRequest){const u=await currentUser();if(!u||u.banned)return jsonError('Please sign in',401);const q=req.nextUrl.searchParams.get('q')||'';const chats=await db.chat.findMany({where:{userId:u.id,...(q?{title:{contains:q,mode:'insensitive' as const}}:{})},orderBy:[{pinned:'desc'},{updatedAt:'desc'}],take:100,select:{id:true,title:true,pinned:true,favorite:true,updatedAt:true}});return NextResponse.json({chats});}
export async function POST(){const u=await currentUser();if(!u||u.banned)return jsonError('Please sign in',401);const chat=await db.chat.create({data:{userId:u.id}});return NextResponse.json({chat});}
