import { currentUser, isPreviewAdmin } from '@/lib/auth'; import { db } from '@/lib/db'; import { redirect } from 'next/navigation'; import ChatApp from '@/components/chat-app';
export default async function Home(){const user=await currentUser();if(!user||user.banned)redirect('/login');if(isPreviewAdmin(user))return <ChatApp user={user} initialChats={[]} settings={{model:'Zyntra v5',temperature:0.7,maxTokens:2048,topP:1,systemPrompt:'You are a helpful assistant.'}}/>;const [chats,settings]=await Promise.all([db.chat.findMany({where:{userId:user.id},orderBy:[{pinned:'desc'},{updatedAt:'desc'}],take:100,select:{id:true,title:true,pinned:true,favorite:true,updatedAt:true}}),db.setting.findUnique({where:{userId:user.id}})]);return <ChatApp user={user} initialChats={chats} settings={settings}/>}
