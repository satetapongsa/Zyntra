'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';import Link from 'next/link';
export default function AuthForm({mode}:{mode:'login'|'register'}){const[error,setError]=useState(''),[busy,setBusy]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch(`/api/auth/${mode}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();if(!r.ok){setError(j.error||'Something went wrong');setBusy(false);return}router.push('/');router.refresh()}return <main className="min-h-screen flex items-center justify-center p-5"><form onSubmit={submit} className="w-full max-w-sm rounded-2xl border border-[#262830] bg-[#111318] p-8"><div className="mb-8 flex items-center gap-2"><span className="text-2xl text-[#d2f36b]">✳</span><span className="text-xl font-semibold">Zyntra</span></div><h1 className="text-2xl font-semibold">{mode==='login'?'Welcome back':'Create your account'}</h1><p className="mt-2 mb-6 text-sm text-[#9699a3]">{mode==='login'?'Sign in to continue to your workspace.':'Start a more thoughtful conversation.'}</p>{mode==='register'&&<label className="mb-4 block text-sm">Name<input name="name" required maxLength={80} className="mt-2 w-full rounded-lg border border-[#292b31] bg-[#0b0c0f] px-3 py-3 outline-none focus:border-[#d2f36b]"/></label>}<label className="mb-4 block text-sm">Email<input name="email" type="email" required className="mt-2 w-full rounded-lg border border-[#292b31] bg-[#0b0c0f] px-3 py-3 outline-none focus:border-[#d2f36b]"/></label><label className="block text-sm">Password<input name="password" type="password" minLength={mode==='register'?10:1} required className="mt-2 w-full rounded-lg border border-[#292b31] bg-[#0b0c0f] px-3 py-3 outline-none focus:border-[#d2f36b]"/></label>{error&&<p className="mt-4 text-sm text-rose-400">{error}</p>}<button disabled={busy} className="mt-6 w-full rounded-lg bg-[#d2f36b] py-3 font-semibold text-[#12140c] disabled:opacity-50">{busy?'Please wait…':mode==='login'?'Sign in':'Create account'}</button><p className="mt-6 text-center text-sm text-[#9699a3]">{mode==='login'?"Don't have an account? ":'Already have an account? '}<Link className="text-[#d2f36b]" href={mode==='login'?'/register':'/login'}>{mode==='login'?'Create one':'Sign in'}</Link></p></form></main>}
